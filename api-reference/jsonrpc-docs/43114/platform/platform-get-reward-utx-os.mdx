---
title: 'platform.getRewardUTXOs'
openapi: 'POST /json-rpc'
playground: 'interactive'
---
### Parameters

Returns UTXOs rewarded after the staking or delegation period ended for the given transaction. Deprecated as of v1.9.12.

- `txID` (string): ID of the staking or delegating transaction.
- `encoding` (string): Format for returned UTXOs. Only "hex" is supported if provided.

### Returns

<ResponseField body='result' type='object'>
  The JSON-RPC result.
</ResponseField>

<RequestExample>

```bash Curl
curl --request POST \
  --url https://api.uniblock.dev/uni/v1/json-rpc?chainId=43114 \
  --header 'content-type: application/json' \
  --data '{
    "id": 1,
    "jsonrpc": "2.0",
    "method": "platform.getRewardUTXOs",
    "params": {}
  }'
```

```json JSON-RPC
{
  "id": 1,
  "jsonrpc": "2.0",
  "method": "platform.getRewardUTXOs",
  "params": {}
}
```

</RequestExample>
<ResponseExample>

```json Success
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {}
}
```

```json Error
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32600,
    "message": "Invalid Request"
  }
}
```

</ResponseExample>
