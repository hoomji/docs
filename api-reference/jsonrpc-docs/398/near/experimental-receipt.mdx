---
title: 'EXPERIMENTAL_receipt'
openapi: 'POST /json-rpc'
playground: 'interactive'
---
### Parameters

Returns the original receipt object by ID, without execution outcome or status.

- `receipt_id` (string): Base58-encoded receipt ID. You can find valid IDs via transaction status results.

### Returns

<ResponseField body='result' type='object'>
  Returns the raw receipt object for a given receipt ID.
</ResponseField>

<RequestExample>

```bash Curl
curl --request POST \
  --url https://api.uniblock.dev/uni/v1/json-rpc?chainId=398 \
  --header 'content-type: application/json' \
  --data '{
    "id": 1,
    "jsonrpc": "2.0",
    "method": "EXPERIMENTAL_receipt",
    "params": {}
  }'
```

```json JSON-RPC
{
  "id": 1,
  "jsonrpc": "2.0",
  "method": "EXPERIMENTAL_receipt",
  "params": {}
}
```

</RequestExample>
<ResponseExample>

```json Success
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {}
}
```

```json Error
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32600,
    "message": "Invalid Request"
  }
}
```

</ResponseExample>
